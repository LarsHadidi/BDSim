#pragma once

#include <interfaces/Sampler.h>

namespace samplers {

    // TODO

    /*class BufferedBinarySampler : public interfaces::Sampler {
    public:
        BufferedBinarySampler();
        ~BufferedBinarySampler();
        virtual void reset();
        virtual void sample();
    protected:
        const double CALLS_PER_SAMPLE;
        double smpCalls;
        std::ofstream outStream;

        const uint32_t BUFFER_SIZE;
        T** buffer;
        uint32_t bufferPosition;

        virtual void writeBufferUntilPos();
    };*/
}

